# Dependencies / build output (belt-and-braces; app/.gitignore also covers these)
node_modules/
.next/
out/
build/
dist/

# Env files — app/.env holds DATABASE_URL. Never commit.
.env
.env.*
!.env.example

# Design canvas review artifact: 2.5 MB single-file HTML regenerated from the
# .dc.html artboards by the design skill. The artboards are the source; this is not.
design/trajectory-ui.html

# OS / editor cruft
.DS_Store
*.tsbuildinfo
next-env.d.ts
